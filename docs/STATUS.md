# kumi 状态与路线

本页区分已有机制、可复现检查和仍待验证的行为。普通接入从[部署说明](../INTEGRATION.md)开始；
运行包使用 schema version 1 的轮次契约。

**2026-09-24 统一规划核对至 `d8ad47e`：全部顺序、依赖、持续事项检查点和下一单元，
只在[综合实施计划](IMPLEMENTATION-PLAN.md#下一阶段执行队列)维护。**
本页保留已有能力、历史验证和问题编号；各专题保留规格与证据，不再维护不同的“下一步”。

本次整合纳入：受控通知与接收方恢复、持久未读/延期防遗忘、项目会话与原生历史管理、
可替换审批后端/人类限时窗口、独立验证与直接返工、显式接班、跨模块协作、真实计量和
开源分发。纯文件消息不等完整审批；接班安排在复杂 M3/M4 前；原生控制、B 的 30 秒
响应目标和计量缺口各有独立检查点，不因通知成功而关闭。

当前重要边界：

- M0 成员发现和 M1a 文件消息首版仅在实验分支（当前基于 `c5d3993`）；main 尚无消息、提醒、通知或防遗忘收口。实验首版已通过 7 项消息回归和全仓库 647 项回归，但不等于可运行包能力。
- AP 只有通用开发用影子工具；真实模型质量评测、审批持久队列与自动执行尚未完成。
- UX-01 页面已交付；UX-02a 有显式 run 的只读目录 CLI、`session-registry` 身份关系 sidecar 和 `capability-registry` 能力证据 sidecar。能力证据 v2 固定 SHA-256，失效/旧 v1 有效状态降为 unknown；同轮多登记显示歧义，缺运行时检查/adapter 时 eligible 保持 false；原生历史隔离/归档/恢复尚未实现；旧历史未被整理。
- CQ-01 有最小工具闭环，完整原生覆盖和成本结论仍未具备；辅助计量继续按原版宿主扩展。
- 四宿主/五配置同等纳入协议和矩阵，未测保持未测。本机 omp 18.3.0 仅有前轮帮助证据，
  不能沿用旧版原生验收。版本事实见[宿主审计](HOST-COMPATIBILITY-20260923.md)。

本轮用独立 sandbox CLI 与六 skills 复制包复现/修复预览缺陷，详见[验证记录](SESSION-PREVIEW-VALIDATION.md)。
没有启动真实宿主/模型；不改变宿主配置、日常安装或运行服务。
详细技术规格见 [M1](M1-IMPLEMENTATION-PLAN.md)、[UX-02](SESSION-MANAGEMENT.md)和
[判定报告跟进](DECISION-LAYER-FOLLOWUP.md)；所有新功能均按各自证据晋级。

## 已有能力

| 能力 | 当前支持 | 边界 |
|---|---|---|
| 六个 skills 独立分发 | 调度、受控协议、四类宿主字典，以及完整的脚本和参考资料 | 无需额外管理仓库；宿主 CLI、认证和终端传输单独安装 |
| herdr 页面组织 | 确认父页面后默认同 space 新 tab、任务＋agent 标题与父页面焦点保留 | UX-01 runner/运行包及限定桌面 viewer 验证完成；窄窗口长标题会截断，页面不隔离文件系统 |
| 提交和恢复 | run/job/round、唯一活动轮、提交 uncertain、租约、跨 watch 待办、分页摘要及可选增量续读 | uncertain 需核对实际证据，不能自动重发；delta 复用源内容，仍扫描目录与验证元数据 |
| 并行监督 | 提交前建立观察、工作预算、提前续接边界、权限画面复核、持久处理回执及可选业务日志检查点等待 | 不会唤醒已结束的调度会话；原生审批 30 秒响应目标仍未通过 |
| 独立交付验证 | 源码快照、独立验收、返工和原生宿主收尾门禁 | 验收内容仍须根据具体任务设计 |
| 用量审计 | 原生来源导入、不可变纠正、轮次选择与来源绑定 | 不保证所有隐式后台调用均被记录 |
| Hermes | 原版宿主编排、可选主循环用量插件、原生累计只读视图及前置诊断 | 辅助逐调用、轮次归属与全宿主完整计量仍有缺口 |

OpenCode/OMO 等字典保留已核对版本及配置相关的兼容说明，用作适配参考；读者应核对自己的版本、插件和实际角色列表，不照搬另一环境的默认配置。运行包不携带模型凭据或个人配置。

## 可复现验证

2026-09-24 AP-01/02 首批：已交付[通用审批后端与影子工具](APPROVAL-SHADOW.md)，
规则、可选 Jev 和独立程序 JSON adapter 共用流程；没有服务商绑定或统一置信度阈值。
**675 项回归全部通过，含 15 项真实 tmux，零失败/跳过**；新增 33 项影子协议回归，
无新模型/API 调用。分发检查仍为 6 个 skills、32 份 Markdown、157 个本地链接，
零发现；新增 Python 文件通过 3.10 语法与 Ruff 检查，实际解释器 3.14.7。
独立 sandbox 的规则演练保留 15 条结果，跨进程 replay/report 完全一致、0 调用/原生动作。
规则仅明确拒绝或升级复核，因此 8/15 标签一致、0 次建议允许，不能用零误放行证明
模型有效。Jev 真实可达性/费用/质量未测；Laya/Von 包装器、完整审批身份/期限、
三级审批闭环与 30 秒响应目标仍开放。本批工具只在 tests，不需要同步运行 skills。

2026-09-24 main **642 项回归全部通过，含 15 项真实 tmux，零失败/跳过**；新增 26 项
审计回归，无新原生模型调用。最小复制运行包 CLI 通过，分发检查为 6 个 skills、32 份
Markdown、157 个本地链接、零发现；Python 3.10 语法检查通过，实际解释器为 3.14.7。

2026-09-24 main 新增 [B3 监督时效审计](B3-SUPERVISION-AUDIT.md)：运行 helper 区分首次
复核、动作证据记账和解决声明，完整历史统计不随分页变化。旧 B2 真实 run 的只读复核
检出 11 条超时 attention 和 10 条 handled 无解决证据；这些不是 11 次独立审批，
也不是本批加速结果。具体门槛与顺序见[主线收口队列](IMPLEMENTATION-PLAN.md#2026-09-24-主线收口与实验分支分开推进)。

2026-09-23 四宿主统一实验入口后，**616 项回归全部通过，含 15 项真实 tmux，零失败/跳过**。
新增 6 项共用契约回归及 1 项五配置真实终端夹具验证：统一检查身份、控制证据、原生预检、
配置变更与未测项；实际启动/绑定/退出五个确定性目标，旁侧 sentinel 保留。独立 sandbox
演练导入五条合成 sample，换进程续采为零新增、五条重复，来源缺口仍可见。Hermes 测试
使用可选主循环 recorder 的合成 SQLite，保留辅助调用等未知范围，不代表原版精确计量完整。
这批零原生模型调用，四宿主新版原生控制和通知矩阵仍未完成；运行 skills 未改。

正式回归入口在[测试说明](../tests/README.md)。默认执行不调用模型；真实 tmux 测试需要显式启用以及本地 socket 权限。2026-09-21 在 Linux、Python 3.14.7 下验证本公开快照：**492 项全部通过，含 13 项真实 tmux 测试，零跳过/失败**。终端测试使用自有临时 socket，在允许本地 socket 的环境中执行；回归没有新模型调用。C1 新增 12 项检查，C2 新增 18 项；规模与边界见[恢复摘要验证](C1-RECOVERY.md)及[增量恢复验证](C2-RECOVERY.md)。B1 另做了真实 omp/Codex 长时实验并新增四项恢复目录回归，详见[监督实测与未通过项](B1-SUPERVISION.md)。B2 新增 18 项回归，完成独立应用和一次短原生验证；[B2 报告](B2-SUPERVISION.md)保留未达标时效和初始观察器到期记录。

2026-09-23 最小控制实验基础后 **609 项全回归通过，含 14 项真实 tmux，无失败/跳过**，其中新增 26 项实验协议回归。独立 sandbox 演练以两类合成 native 日志格式完成 7 项检查、无模型调用；synthetic samples 只证明导入与恢复路径，不算真实用量。资源冲突、固定输入/身份、失败恢复、通知与停止证据分离已验证。Codex/omp 新版原生控制/通知能力尚未在这批实测；真实计量仍需按 run 绑定实际日志。

2026-09-23 UX-01 后全套回归 **538 项全部通过，含 13 项真实 tmux，零跳过/失败**；独立 herdr 服务与 kitty/niri viewer 验证通过，窄 tab 栏显示限制见[页面体验报告](UX1-PAGES.md)。

DH-02/03 首批后 **570 项全部通过，含 14 项真实 tmux，零跳过/失败**；新增控制流程通过真实 CLI 验证，详见[首批报告](DH23-CONTROLS.md)。

同日[滚动更新兼容审计](HOST-COMPATIBILITY-20260923.md)后 **583 项全部通过，含 14 项真实 tmux，零跳过/失败**。本机 Codex 0.155.1、omp 18.2.11 的必要 CLI 参数仍可用；Hermes 0.21.3 主循环插件重验通过，另完成 14 项原生审批 transport 合成检查。新版辅助 hook 仅做能力识别，尚无对应采集器；本轮零新模型调用，不代替四宿主原生拒绝/停止矩阵。

本次分发包检查为 6 个 skills、32 份 Markdown、156 个本地链接、零发现。公开树另做了本地链接解析、Bash 示例语法及个人路径/明显凭据模式检查；合成测试值保留。该内容审阅使用有限规则和人工核对，不是完整的自动密钥检测证明。

CI 配置覆盖 Python 3.10–3.14；配置存在不等于远端任务已通过。可选宿主探针和实际模型 E2E 需单独执行。包内链接检查也不是全部宿主行为已验收的证明。

## 主线顺序：C → B → E

以下保留 C/B/E 既有工作分类和历史进度，不是另一套当前排期；执行以综合计划为准。

| 顺序 | 工作 | 当前状态 | 验收要求 |
|---|---|---|---|
| C | 精简恢复输出与增量读取 | C1/C2 首版完成：summary 分页；delta 复用源内容并按变化续读，待办/错误/健康每次返回 | 已比较 1/10/50/100 任务的完整/摘要/增量；验证权限、waiting_user、未知健康、部分失败、游标失效与仅随时间发生的过期；缓存自身 I/O 单独计量，未宣称普遍加速 |
| B | 长时并行监督、审批与恢复 | B1 长时实测后，B2 检查点等待、stdin 发布、60 秒续接余量及旧 blocked 复核已实现；短原生功能通过，审批响应仍超 30 秒 | 进入用户真实项目复测，保留完整时序；长期多宿主处置、重叠接续与写入范围继续验收，不把工具实现或单次功能通过当作整个 B 完成 |
| E | 同任务单／多 agent 对照 | 待前置完成 | 相同任务和验收，计入调度方、子任务、后台、重试与返工；比较质量、token、墙钟时间、等待和波动 |

B 的时效与监督仍未收口，已在综合计划设置原生基线、消息实验和审批修复后的检查点；E 的基线从原生实验开始保留，不等最后补账。`recover --summary` 与 `supervision.py` 仍读取完整 recover；delta 缓存只减少源内容重复读取和行输出，目录扫描、元数据检查、JSON 验证及时间判断继续执行，并写入私有游标，尚非常数成本查询。默认完整 recover 的 `--limit` 仍只分页待办。摘要或 delta 健康计数均不替代监督检查。输出字节数不能直接换算为 token 或费用，当前没有多 agent 净节省 token 的结论。

2026-09-23 已接收 DataHive 实际项目反馈，逐项核对与拟定实施队列见
[DataHive 反馈与页面体验待办](DATAHIVE-FOLLOWUP.md)。优先收敛按轮验收、取消/接管、
审批关联，并记录同 space 新 tab 与“任务目的＋agent 类型”标题需求。DH-01 已交付，
验收与切轮规则见[按轮验收报告](DH1-ROUND-ACCEPTANCE.md)；UX-01 页面实现及限定 viewer 验证也已完成。

同日另记录[网状协作演进提案](MESH-COLLABORATION.md)：从同伴直接问答、作者与验证者
协作，逐步到限定委托和跨模块协调。M0 成员发现已在 `experiment/peer-notices` 的 `c5d3993`
实现，未合并 main；消息契约及 M1–M4 仍待做，不替代 B 主线。消息读取/唤醒、
唯一输入控制方、按版本验收与权限巡视分别设计，不把新增聊天通道当作完成自治编排。
本批共同实验入口位于 `tests/control_lab.py` 与 `tests/lab_resources.py`，属于开发工具，
不随 skills 安装，也不执行 native 终端控制。实验分支成员目录通过 24 项专项检查，
实验分支总回归 640 项；不能把该数量计作 main 的回归或消息上线证据。
已补充后续独立分支实验方向：文件保存正文，终端仅通知发送方／优先级／短主题／ID，
接收方先核查是否需要恢复，再自主读或持久延期后继续工作；允许可解释的中断，
未知工具结果不盲目重放。通知不强制立即处理，尚未实现该通道。

进一步的[DataHive 成本与质量复核](DATAHIVE-COST-QUALITY.md)已读取原生用量：两任调度
会话存在明显的累计上下文成本，但现场未形成按轮用量账本。新增 CQ-01–CQ-06，优先
补计量闭环、按风险委托验收、阶段接班和早期契约验证；网状拓扑与换会话均未证明净节省。

## 既有事项对照

本表延续既有工作编号，避免将阶段性实现误当成全部完成，或把已完成事项重新列为待办。A–E 的含义为：A 交付配置、B 长时监督、C 恢复输出、D 计量、E 对照实验；早期报告中同字母的阶段标题不作为当前编号。C1/C2 是 C 的实施拆分，实际排期见综合计划。

| 既有编号 | 已完成的部分 | 剩余事项／当前归属 |
|---|---|---|
| F01 身份与页面 | herdr 资源 ID 校验、任务命名、新 workspace/tab 和归属约束 | UX-01 页面已交付；UX-02 原生历史收纳、项目会话目录与恢复待实现；多桌面/原生宿主长期体验待验证 |
| F02/F03 提交与恢复 | 本地回执、uncertain、租约、唯一活动轮、持久 run、跨 watch 待办 | C 的输出与历史规模；B/F10 的长期接续和原生故障组合 |
| F04 权限与巡视 | 降噪、对话关联、已知正文误报修正、逐项处理回执 | B 的长期处置时效，以及更多宿主 UI 的识别覆盖 |
| F05/F06/F09 交付与生命周期 | 固定快照、独立验收、宿主收尾门禁、作者/验证/返工模板 | F10 中各宿主后台活动和交付流程的组合验收；机制不能代替实际观察 |
| A 交付配置验收 | 交付配置/未覆盖项配方、发布配置负对照、拒收与新轮返工演练 | 已完成该批限定验收；任意任务的验收完整性不会自动推断 |
| W5-U1/U2/U3/U5 | 缺基线/结果告警、发布后纠错出口、验证轮自己的验收示例、文件类别说明 | 已落地；后续保留回归 |
| W5-U4 角色成本 | role/phase 与 attempt purpose 分离、分组及显式来源覆盖 | 基础已完成，完整计量与复盘仍归 D/F08 |
| U6/U7/U8 | 同 job 不可变用量纠正、OpenCode 已完成轮选择导入、run/native 来源发现与显式绑定 | 已完成各自限定范围；跨 job/来源别名和隐式来源仍是缺口 |
| D / F07/F08 | 四宿主已有对应读取层次，提供角色分组、来源审计和未知覆盖 | Hermes 精确辅助计量、跨 job 归属、来源重叠、自动复盘与费用口径尚未整体验收 |
| U9 / H1–H4 Hermes | H1 前置诊断、主循环插件、累计只读适配；H2 有接口提案草稿；H3 默认原版路线已落地 | H2 尚未对外提交；原生扩展、版本兼容和 H4 剩余计量路径继续跟进，本体补丁不恢复为默认接入 |
| F10 回归与真实断点 | 当前 runner、确定性回归，以及限定版本/场景的真实宿主验证 | 四宿主故障组合及长期覆盖未完成，包含下方矩阵和 B；不凭回归总数关闭此项 |
| F11 / E 成本对照 | 已有任务、独立验收与计量基础工具 | 对照实验未实施，尚无净节省结论 |
| R1–R5 读取与巡视修复 | 文件读取边界、损坏 publication 后的独立 UI 观察、请求固定、结果与指纹一致性已补；旧实验输入入口未分发 | 当前 runner 另有就绪与监督门槛；不把旧报告的修复前状态重新列为现存缺陷 |

这些编号用于衔接工作，不要求使用者读取私人实验记录。可公开复现的依据在[测试索引](../tests/README.md)、[当前 E2E 配方](../tests/E2E.md)及运行包内。

## 独立跟进与后续积压

- [ ] **V-01–V-04 宿主更新跟进**：版本/CLI 漂移探针与 Hermes 原生审批接口合成验证已交付；待做新版 Codex/omp 原生控制、上游 Hermes 辅助 hook 适配、人类审批 transport 桥接及 Codex 0.156.x/OpenCode/OMO 新组合矩阵。详见[审计与执行项](HOST-COMPATIBILITY-20260923.md)，不自动更新用户安装。
- [x] **DH-01 按轮验收与切轮检查**：历史轮查询/补记、切轮时状态留痕、success 接受/拒收检查、blocked/error 正常续轮及关闭后仅验收租约已交付；[验证与兼容说明](DH1-ROUND-ACCEPTANCE.md)。
- [x] **CQ-01 最小计量工具闭环**：不可变 run 计量计划、显式阶段采集与来源审计、失败报告和幂等续采、逐调用非缓存输入已实现；[验证与边界](CQ1-METERING.md)。真实项目连续计量和成本/审批基线仍待做，未关闭整个 CQ-01。
- [ ] **AP-01–AP-06 三级审批**：可选决策后端分流、专门协作者复核、人类限时窗口、超时拒绝、宿主精确执行、反馈与有界重提；依赖 DH-02/03 的相关门槛，先影子评测再启用限定类别。Jev/Laya/Von/Foq 等的优先级和验收条件见[选型报告](APPROVAL-MODEL-SELECTION.md)，完整规格与批次见[综合计划](IMPLEMENTATION-PLAN.md)。尚无运行中的自动审批服务。
- [x] **DH-02/03 控制基础首批**：完整捕获证据、事件指纹、租约下动作意图/确认、原事件 review、接管保留不确定性与无结果取消门槛已交付；[验证及兼容变化](DH23-CONTROLS.md)。宿主执行器与全矩阵尚未完成。
- [ ] **DataHive DH-02–DH-07**：取消/预算/接管、审批关联与真实记账、参数化工作流、轻量验收、原生身份、输入版本及测试资源占用。DH-01 已单独交付；原反馈编号、现有保护和现场脚本问题分别记于[跟进清单](DATAHIVE-FOLLOWUP.md)，不直接照搬临时自动审批器。
- [x] **UX-01 同 space 新 tab 与 agent 标识**：确认父空间后默认新增 tab，保持 no-focus；标题如“修复重试逻辑 · Codex”，重名加短编号；显式新 space/分屏仍可选，续轮保留原页。runner、归属校验和限定桌面 viewer 验证完成，详见[页面体验验证](UX1-PAGES.md)。
- [ ] **UX-02 会话管理**：只读项目目录、新 worker 历史策略、受控归档与精确恢复、旧历史盘点/显式收纳；默认宿主选择器和 kumi 分组分别验收，消息/审批/计量证据不断档。详见[规格](SESSION-MANAGEMENT.md)，综合阶段 2/4/5 及后续旧历史小批推进。
- [ ] **M0–M4 网状协作**：M0 成员发现已在实验分支实现，未合并 main；待做消息协议与读取检查点、同伴问答、委托验证、跨模块协调及成本对照；设计与验收边界见[提案](MESH-COLLABORATION.md)。问答原型可独立推进，自动返工/控制权委托依赖 DH-01/02/03/07 的相关门槛；尚未新增运行能力。
- [ ] **CQ-01–CQ-06 成本与质量优化**：实际运行计量（含调度方和原生子来源）、按风险委托验收、阶段接班、早期契约验证、问题聚合与停止条件、同任务对照。见[实测复核与接班设计](DATAHIVE-COST-QUALITY.md)；不以缓存 token 总数推导费用，不将历史缺陷当作当前未修复项。
- [x] **B2 检查点等待与接续工具**：只读日志 waiter、60 秒默认余量、`renew_by` 已实现；独立应用与短原生场景通过，后一次绑定提前 67.42 秒。日志前提及初始到期记录见 B2 报告。
- [x] **B2 准确发布配方**：生成 JSON argv，严格 stdin 输入并从 request 定位结果；原生场景一次发布、无候选文件。B1 原有违规证据继续保留。
- [x] **B2 旧 blocked 回归与事件区分**：无提示的 blocked 立即要求人工复核，不再伪称新 attention；重绘、真实提示重现和盲读恢复有回归，输入门槛未放宽。
- [x] **B3 只读监督时效审计**：阶段复盘 helper、坏时钟/证据/历史/分页校验及旧真实 run 复核已交付；不改变巡视或执行速度，不关闭 B2 时效目标。
- [ ] **B2 真实项目时效与范围复测**：短原生审批发现至动作仍为 100.67 秒；等待 helper 不解决控制器往返延迟。复测长时多宿主、提前续接、路径及额外临时写入；关注 PID 命名空间误判、多余输出、游标重复读取和手写 shell 发布配方。30 秒目标未通过，不关闭整个 B。
- [ ] **F10 原生组合矩阵**：按 Codex、omp、Hermes、OpenCode/OMO 与 pure 记录版本和配置，补成熟资源 ID、提交超时/回执丢失、blocked 续轮、换 watch/控制器恢复、后台取消与收尾、交付及用量重导的组合覆盖；已有单场景证据不重复冒充全矩阵验收。
- [ ] **D/F07/F08 跨来源归属**：跨 job 的迁移/撤销、来源别名、复制/分叉历史重叠、隐式控制器和后台来源盘点。冲突保持显式，未知保持未知；不能用新 sample ID 绕过重复计数。
- [ ] **D/F08 自动复盘与费用口径**：从明确时间窗和事件计算返工/复验、并发与审批等待指标，区分协议、验收、宿主和来源覆盖；费用需明确模型与价格版本，角色分组本身不等于实际账单。E 可先选择覆盖可解释的宿主子集，不以所有宿主全量计量为前提。
- [ ] **U9/H2–H4 Hermes 原生扩展／Relay**：先解决落盘前只保留数值的观察器与可信身份映射，再验证异步、流式、重试、嵌套、后台、退出 flush、重复记录和丢事件；接口提案未对外提交，日常安装不启用 Relay。
- [ ] **Hermes 原生错误／重试**：补真实 provider 错误路径；确定性模拟不代替现场覆盖。
- [ ] **结果发布文件落点持续验证**：B2 已提供无候选文件的 stdin 路线；继续核对真实模型是否准确应用生成 argv，以及范围外副作用，不能依靠提示配方证明强制隔离。
- [ ] **F01 herdr 桌面 viewer 扩展**：UX-01 独立 kitty/niri 的名称、切页和焦点检查已完成；补其他尺寸/桌面与长期原生宿主交互，服务器布局证据单独保留。

发布维护与功能待办分开：许可证、公开包和部署/使用文档已具备；远端 CI 成功运行、实际安装发现及宿主版本兼容仍须各自验收。目录命名整理不改变 B/C/D/E 的完成状态。

本项目目前适合有持续监督责任的本地协作，不提供完整的无人值守调度服务。
综合计划新增可选的持久审批服务与有限宿主通知适配，均待实施；它们不等于通用的自动唤醒或无人值守调度。
系统级写者归因与强制文件隔离仍属另外的工程范围，不视为当前清单中已实现的能力。
