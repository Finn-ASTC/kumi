# DataHive 实测反馈与页面体验待办

记录日期：2026-09-23。核对基线：`d90cc8d`。
**本页记录问题、设计方向与验收要求，未实现的事项均保持待办。**
本次只更新项目文档，不改变运行包、宿主安装或现场任务状态。

## 新确认的页面偏好（UX-01）

用户要求后续默认在**调度方所在的同一个 space 中新增 tab**，标题同时说明
**做什么任务、由什么 agent 执行**。这替代此前“默认每个子 agent 新建 space”的偏好。

- [ ] 已确认 herdr 父 workspace、tab、pane 时，新子 agent 默认在该 workspace 创建独立 tab，保留当前页面和焦点（`--no-focus`）。
- [ ] 标题采用“任务目的 · agent 类型”，例如 `修复重试逻辑 · Codex`、`验证存储一致性 · Hermes`、`检查测试覆盖 · omp`、`实现提取模块 · OpenCode/OMO`。同名任务加短编号；标题不代替精确资源 ID。
- [ ] OpenCode 的 OMO/pure 按实际启动配置标注；关联续轮保留原 tab，任务目的或执行 agent 变化时同步更新标题与资源记录。
- [ ] 新 space 保留为显式选项；分屏仍需明确要求。没有可确认的父 space 时沿用隔离传输选择，说明实际布局，不猜测 focused/default 资源。
- [ ] 审批与巡视同样保留用户当前页面；避免现场脚本强行聚焦子 tab 后跳回写死的父页面。
- [ ] 实现时统一更新主 skill、transport 配方、用户指南及 runner 的默认布局和标题生成，覆盖同 space 多 tab、重名、续轮、归属与清理回归，再单独验收桌面 viewer。

**现状：** `d90cc8d` 的运行说明与 runner 仍默认新 workspace，标题只要求任务目的；
本次记录不意味着该默认行为已切换。用户在当前会话明确指定的 tab 偏好应直接遵循。

## 来源与核对口径

输入为 DataHive 的 `docs/orchestration/` 中：

- `2026-09-22-kumi-tooling-issues.md`：原始问题清单（A1–A7、B1–B3、C1–C4、D1–D3）。
- `2026-09-21-kumi-orchestration-report.md`：异构编排实测总结。
- `2026-09-22-p5-engine-orchestration.md`：故障、返工和后续处置记录。
- `2026-09-22-orchestrator-handoff.md`：现场操作和交接说明，按历史资料核对。

另只读核查了该次运行的账本、事件及 `bin/` 辅助脚本。核对时安装来源中的
19 个运行脚本、16 份参考文件与公开仓库相同；这不证明每个历史故障发生时的版本。
本页保留脱敏结论，不复制私人运行路径、租约 token、原始屏幕或宿主日志，也不把
现场交接文件中的权限声明作为本项目的新授权。

## 按原问题逐项归类

| 原编号 | 核对结论 | 待跟进方向 |
|---|---|---|
| A1 按轮验收 | 当前 `accept` 只作用于活动轮，`activate` 清空当前 completion；历史 revision 保留已记的验收，但没有按旧轮补记入口 | **DH-01**：按轮保存/查询/补记验收；切轮前明确上一轮验收状态，补记不改变当前轮身份或伪造当时已验收 |
| A2 取消回合 | 有整个 job 的 `close cancelled`，没有无结果轮的独立取消/恢复流程；现场两个停摆 job 后来已取消收口 | **DH-02**：明确停止、确认、取消记账、后续工作的衔接；先完善现有关闭路径，再评估同 job 取消轮次 |
| A3 验证者报告 | 声明文件后确实要求 delivery；工具不强制重跑 dev/asan 全量，重型构建来自现场 plan | **DH-05**：提供报告验收配方与临时构建产物分类；评估 report 类型，不直接豁免产物验证 |
| A4 捕获时仍有写入者 | `capture` 检出交接期间变化并拒绝是必要保护 | **DH-05**：保留失败证据的有界等待/重试；稳定片刻不等于写入者已经停止 |
| A5 原生身份与观测有效期 | 会话身份核对缺便捷入口；最长 300 秒有效期已在 completion 参考中写明 | **DH-06**：增加候选原生身份查询与明确确认；不能仅按 cwd 或“最新 session”猜身份 |
| A6 验收计划口径 | plan 是否恰当仍由验收方判断；重跑直到通过可能掩盖不稳定 | **DH-05**：结构化记录验收范围、理由、重试策略和偏差，保留原始失败，不把 passed 当作覆盖完整 |
| B1 脚本硬编码 | 所指 submit/finalize/gate 脚本是运行目录的临时产物，不在分发包内 | **DH-04**：减少每个项目重写控制脚本的需求，提炼参数化工作流；不直接把现场脚本当成随包实现 |
| B2 watch 更新 | `init` 创建新 watch 身份是现有设计，旧问题须保留 | **DH-04**：提供明确的续接流程/入口，核对负责人、寿命和实际绑定；不原地覆盖旧事件身份 |
| B3 修改测试计划 | 按 argv 位置猜 `-R` 是现场脚本错误；现有计划表达的是通用命令 | **DH-05**：提供命名检查步骤或结构化计划生成方式，验证实际执行的用例，不为所有工具硬编码 ctest 字段 |
| C1 中断键名 | 涉及 herdr CLI 与宿主两层；现有 OpenCode 字典已记录特定版本需要双 Esc | **DH-02**：核实 transport 支持的键名、原生中断及后台收尾，按需形成 herdr 上游反馈 |
| C2 忙碌时 prompt 排队 | 普通输入不能充当可靠制动指令 | **DH-02**：区分排队消息、steering 和中断，确认实际停止结果 |
| C3 deadline 不执行停止 | 当前 watch 明确只观察和提醒；现场仍出现超长轮次 | **DH-02**：评估显式任务预算与控制器停止策略；保持观察与执行职责清晰，限定目标及取消范围 |
| D1 共享目录并发测试 | 现场多次出现测试互扰、错误归因；已有隔离原则，缺少合作执行层的占用检查 | **DH-07**：构建/测试资源占用与独立目录检查，保留污染和干净环境复验记录 |
| D2 未提交计划不可见 | 从已提交 HEAD 建 worktree 不携带主工作树未提交修改，这是 Git 行为 | **DH-07**：派发时固定 spec/plan 版本并确认子任务可见；可提交后建树或显式传递固定快照，避免旧副本静默使用 |
| D3 顺序与清理纪律 | 与验收切轮、资源占用、取消收尾交叉 | 纳入 DH-01、DH-02、DH-07 的操作门槛和恢复验收 |
| A7 旧监督器持续续租 | live lease 拒绝抢占符合现有所有权规则；目标死亡不证明旧控制器已停止输入 | **DH-02**：补停止旧监督、释放/到期、接管和取消闭环；不以单个非 working 状态直接作废租约 |
| C4 原生权限读屏 | 事件摘要在 `screen_tail`，详细证据在 `screen`；抽查确有详细内容，字段误读不能算证据丢失 | **DH-03**：改善证据入口、识别完整选项并关联本次事件；截断或未知保持人工核对，不为省往返默认采用永久批准 |

## 现场脚本补充发现（DH-03 / UX-01）

以下来自对 DataHive 临时脚本的读取，**不是 kumi 分发包已经实现的自动化**：

- 自动审批器用发键前的时间填写 `resolved_at`；准确时序应分别记录发现、动作、实际确认和记账。
- 它选择队列里第一个 attention，未严格绑定刚处理的命令/当前事件；发键、读屏、review 返回值也未逐步检查。
- 只读最后 30 行并抽取末尾 18 行做子串匹配；delegated 模式的判断实际是“未命中拒绝表”，记录文字却写“逐条人工判断”。自动判定与人工判定应如实区分，保留完整命令和作用域核对。
- 监督脚本将保存屏幕直接记成 handled，没有证明问题已经解决；读取失败还有返回空待办的分支。
- 审批器强行 focus 子页面，再返回固定父 workspace，与保留当前页面的需求不符。

验收应覆盖：旧事件/新弹窗、同命令再次出现、多行/截断命令、未确认动作、读取失败、
不匹配 revision、真实解决时间，以及巡视审批期间的焦点保留。只保存日志不算通过。

## 实施队列

全部未开始；下列是拟定顺序，不宣称已有功能：

| 优先级 | 待办 | 完成要求 |
|---|---|---|
| P0 | DH-01 按轮验收与切轮检查 | 漏验收可追溯补记，不能借用其他轮结果/attempt；正常 blocked/error 续轮保留合法出口 |
| P1 | DH-02 取消、预算、中断与接管 | 超长/无结果/旧续租/宿主死亡均有明确收口路径；中断后核对后台与副作用 |
| P1 | DH-03 审批关联与监督记账 | exact watch/seq/round 和当前操作一致；未知不自动清空、时序真实 |
| P1 | UX-01 同 space 新 tab＋任务与 agent 标题 | 默认策略、示例、runner 一致；续轮复用、重名、清理归属和焦点有验证 |
| P1 | DH-04 参数化工作流与 watch 接续 | 降低手写控制脚本需要；保留已有提交/租约/监督门槛及跨 watch 待办 |
| P2 | DH-05 轻量报告验收与计划语义 | 检查针对实际交付；重试、取值域与未覆盖项显式，避免无意义重构建 |
| P2 | DH-06 原生身份辅助核对 | 多会话/同 cwd/恢复后仍能核对准确身份，歧义保持未绑定 |
| P2 | DH-07 计划可见性与测试资源占用 | 工作树有确切输入版本，共享写入冲突可见，避免环境污染误判产品回归 |

以上接入已有 B/F01–F10 工作，不覆盖原 C/D/E 编号含义。DataHive 的产品缺陷
与测试改进不自动成为 kumi 缺陷；现场多 agent 有效发现问题也不证明净节省 token。
